[package]
name = "cassette-core"
version = "0.1.0"
edition = "2021"
description = "Core library for standardized WebAssembly cassette interface"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0" 