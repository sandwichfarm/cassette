name: cassette_benchmark
description: Cassette WASM benchmark for Dart
version: 1.0.0

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  cassette_loader:
    path: ../../bindings/dart
  wasm: ^0.2.0

dev_dependencies:
  lints: ^2.0.0